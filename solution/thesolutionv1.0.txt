总体思路 不要一次对全网全时段做 MIP。反复：看当前路由快照找出最热（t,arc）；只把走在这些热3上的 demand 解冻成 MIP变量，其余流量冻成背景负载 H；给这些 demand 生成一批 0/1/2-waypoint 候选并预计算每条路径的 ECMP弧负载：解路径选择 MIP（每个demand 选一条候选）最小化当前子问题的lex饱和；若 first bit下降则接受并清空跳过集合，若只是尾巴更好则接受但继续打同一热弧，若最优仍打不下 first bit则按覆盖度累加置信度、多了就冻結该弧去打下一条；真到 wall-clock 或 focus-N！ 全部做完且搜索规模不能再涨，。这是分解 ＋路径列生成（离线列）+宇典序 MIP剥离+热3调度。3、为什么这样拆 全模型变量是「每条demand x每个t*每条可能路径」，setB 有数干 demand、12个t、上百弧，列数爆炸。观察：宇典序只关心最热的若干（t,arc）；一条热3上通常只有一部分demand；把不走热弧的 demand冻住后负载变常数，MIP只改能挪开热弧的人；候选事先路由好，MIP里每条路径一个0-1变量，负载是线性组合，没有嵌入式最短路。代价：候选必须覆盖能卸热弧的路径，候选空间里没有统路则 MIP薄最优也卸不掉。setA-01 的 arc29:SP-ECMP 下进 sink 14 的最短路全走 29，first bit 0.929383 是结构下界，Sprint 最优也是这个数。4。仓库与目录 challenge-roadef-2026/ 含 setA/ 与 setB/ （net、tm、scenario json）、solution/（求解器）、docs/ 与 howtorun.md、sprint_results/（官方最优向量）.solution/ 含 main.py（单实例 net/tm/scenario-srpaths）、run._setA.py 与 run_setB.py、tools/run_setB_batch.py.tasr/、gurobi/、graph/py/ （numba Graph）、tests/、bench/、out/。 tasr/ 含 data.py（读 json）、routing-py（0ijkstra. ECMP、 SpCache,Hamming）、snapshot.py（稠密负载/饱和与 patch）、eval.py（sat向量、trunc6、lex、可行性）、decompose.py（热弧子问题）、presolve.py（物理桥/outdeg1）、schedule.py （pingpong、focus-N）、budget.py （Hamming 预算）、sticky.py （waypoint 拷到后续冻结槽）、i0.PY（写srpaths）、graph_unit.py （DataC-Graph）、liveview.py （HTML 看板）、candidates/common.py（池、compress、try_keep）、candidates/mix•Py（多策略合并+5%场外随机）、candidates/io.py、candidates/_experimental/ （stretch. od_scan.hot_center、hot_avoid、od_unload. bottleneck. sb_pin.graph_wp） • gurobi/含 env.py （本地 License或 Cluster Manager）、buzLder.py （路位 NLP. LexwB、Hamning、 cover）、.soLver.Py （9N层：调度-分解-MIP-接受/冻结）•5.数据层 tasr/data.py DataC,Load（net,tm,scenario）：节点 id压成 0..n-1；弧含 1d、from/to内部下标、metric.capacityi adj luJ=［（v,metric,arc_id）Joo.J,rev 同理；demand 用内部下标 s/t 与 v［tl;budgetIt］是时段 相对t 1的Hamming上限（t=0为②）；interventions［t】是该时段banned 弧：banned（t）返回 frozenset，路由全部用这个集合。输出 srpaths 时必须把内部节点下标映回原始 node id （data.node_id）。6.路由层 tasr/routing.pyroute_demand（s, t, vol, waypoints, banned, cache, load, used）：段序列为［s］ +waypoints+It］；每一段独立做 metric 最短路 DAG 上的 ECNP（正反向 Dijkstra 禁弧；紧边 dist ［uJ +wtdist_revlv）==dist［sdstJ：按到达该点的最短路条数比例拆流量）；结果緊加到 1oad ［arc_id）， 3 i0 进 used， 这就是官方 SR: waypoint 只定必经节点，段内仍是 SPAECMP，不能用 vaypoint 强迫走比SP更长的边。setA-01 的 11-14 直连 metric436、 11-7-14=380，只要29 不禁，任何以14 为宿的SP 段都会吸回29、 Spcache 按frozenset（banned）缓存正向/反向距离、按距离的正向拓扑序、以及（banned,s,t,waypoint元组）-单位流量弧负载，满了淘汰最老三分之一；候选生成和 snapshot 都打这条缓存。sr_pairs（s, t,wp）是相邻 waypoint（含端点）的节点对集合；demand_hamming 是两套waypoint 的对称差大小；budget_cost（t）是所有 demand 在t-1与t之间的 Hamming之和。7。快照 tasr/snapshot.py RoutingSnapshot 是当前解的可变状态：loadlt,a］ 与 sat（）=load/cap;on_arclt］（a）记录哪些 demand用了该弧（供分解）；frac［tJ［dl是该 demand 单位流量弧负载；wp是当前 waypoint； patch（wp2）只重路由 waypoint变了的 demand 并增量改load，接受/回滚都走 patch。rank_keys（skip,t）按 sat降序返回（t,arC_id），给定t则只排该时段。空 waypoint 时同一（S,t）的 demand 可build_t_dedup 去重路由以加速初始快照。8.评价 tasr/eval.py trunc6 向零截6位。first_bit / first_bit_better 只比第一位。lex_better 比整条 trunc6 向量字典序。evaluate / evaluate_snapshot给出ok（可路由且Hamming合法）、mlu、mlu6、sat、 sat6. budget_cost.接受规则必须用 trunc6后的 lex，不能用原始浮点。9.分解 tasr/decompose.py Subproblem 含 seed （t,arc._id）与一并打的 hot_keys、active_demand_ids（当前走在任一 hot key上的 demand）、frozen（其余 demand 按t的负载）、H（冻结负载在还未ignore的弧上的最大饱和，作为 MIP的2下界；H 算错会出现偷冻结流量的假改进）。dynN: n._hot_min （CLI-first-n-hot，默认 1）到 n_hot_max。same_t_first 先排与 seed 同时段的热弧再排別的七（跨七会引爆 Hamming y 变量）。
choose_nLfrom_prefix 用 snapshot的prefix_pairs 估计「前K 条热弧涉及的（demand,t）对数 × pooL_est」，不超过 mip_budget， 从 n_hot._start 倍增再二分。-grow-mult 与 epoch._step 把本轮 '_hot_max 放大为 int （n_ hot, maxkscale）+epoch_step. allow 曾把分解限制在 focus-N 的 key 集合里；当前 hot 轮和 early 轮都 allow-None,focus-N 只决定打哪个t，该t下仍按 sat 取前 N条弧，否则每个t往往只剩1根柱子。10。候选路径 tasr/candidates/每个策略返回 cands ［tI ［di］ ［kJ（第k 条 waypoint序列）、fraclt］【di］［k］（该路径单位流量 ECNP弧负載）、k_by t t】（策略松弛/跳数标签，只打日志）、pools_by t t］ d 】（用过的中间节点）。MIP看不到 waypoint，只看 frac 当系数。common.py: stretch_pool 为 metric 拉伸 dist（S,u）+dist（u, t） <=dist（s,t） +slack; hop_scan_pool / grow_od_pool 把跳数球涨到 POOL_CAP=24；compress_waypoints 若u 在s-t 最短路上则丢掉；waypoint_seqs（pool, max_w）产出空、单点、再 combinations 的双向两点；try_keep 路由、按负载签名去重、写入袋子。上限约每 demand 24条 1-WP、32条 2-WP，每时段总路径 cap 16000。 DEFAULT_MIX=（"hot_center”，"od_scan”， "bottleneck"）：hot_center 用 OD 跳数球再按到热弧端点的趴数排序，能卸热弧的点才做 2-wp:od_scan纯跳数球不看热弧：battleneck 看当前SP上的热弧、分别 ban一条找绕行点、再两两点对做 2-wp。merge_bags 按负载签名合并；inject_offhot_random每个 demand 再加约5%不在热池里的节点的 1-wp。其它策略在_experimental/，-candidate-mode仍可点名。候选都是不禁热弧的 SP段拼接；若某弧是进某个 sink的度量支配边，候选 frac 在该弧上全是1，MIP无法卸载，这不是 bug而是 SR+SP 模型本身；ban 热弧生成的路径若写进袋子，部署时不ban 会走回热弧，系数和真实路由不一致。11.Hamming 预算 tasr/budget.py MIP 里相邻t的路径差计入 Hamming。
full：每时段用完全部剩余预算。first_half（当前 setB 默认）：第1轮只用剩余的50%，之后100%，避免 round 1 把预算花光。剩余= budget［t】减去冻结 demand B占用的Hamming（冻结 demand 本轮 waypoint不变，跨t差要先扣）。rhs 必须 cLamp 到>=0，否则模型直接 infeasible。跨时段 sticky 时同一X变量管多个t，Hamming变成线性；若左右时段决策变量分属不同 owner，才引入y>=X a+×_b-1。12.Sticky tasr/sticky.py -sticky-span 1：本轮只在 seed 的t上做决策，把选中的 waypoint 原样拷到后面连续的、本轮没有 active demand 的时段（通常 t+1）。目的是一次 MIP同时压相邻槽且Hamming简单。副作用是可能把t的路径强加给t+2、占预算、限制独立优化；setB上整体仍划算。拷过去后因维修走不通的列记入sticky_illegal，不用。13.Presolve tasr/presolve.py 一presolve unmovable —presolve-time 15：按 sat 从热到冷扫 key；若该弧上每个当前 OD在 ban 掉该弧后s-t 不连通则判unmovable：源点在禁弧后出度s1（outdeg1）或图标也冻。冻的是物理标，进 pre_frozen，永久；可 graph.pin_arc使1b=ub=当前负载，并从 max（z）的弧集合拿掉。1b_sat是这些不可动弧的饱和，作为全局下界，first bit 不可能低于它。时间到了就停（t imed _out）•求解中若某 seed 最优仍 miss，再 test_key：若其实是桥，升级为 prefrozen 而不是 soft.14.调度 tasr/schedule.py 无 focus-N 时 PingPong•pick：偶数 round 为 hot（当前全局最热可动弧所在t）；奇数 round 为 early （earlyaT 然后 early+=1， 从 t=0 扫到 T-1）。focus-N 打开后不用这个 pick，但奇偶约定相同。一focus-n 10：每个 epoch 开始时取当前最热的10个可动key（不含 pre_frozen）固定为 epoch_focus。偶数步 focus-hot 打 epoch_focus 里还没 done 的最热 key 的ti奇数步 focus-early用 epoch_early&T 扫时段，不用 focus 集合过滤分解。该 epoch 内 10个 key 都 soft-frozen/skip 完：若search_scale*grow_mult 还能涨则解冻 soft、scale 加倍、n_hot_max 与 mip_budget 加倍、从 t=0重启 pingpong （epoch._step=epoch_early=o）；若已到-scale-cap则停，不要解冻也不要把第11热的弧冻进去。soft_frozen 只对 epoch_focus 里的key。 15.外层循环 gurobi/solver.py 空 SR 建快照并 presolve 得 pre_frozen;while 未到 wall/max_rounds：选 focus_t/seed; scale=max（search._scale，该 seed 的 retry scale）；decompose_hot；解 MIP 得 wp2:patch 评估：first_bit 更好则接受井清空 skip/conf/soft/focus_done 并重建 epoch_focus；仅 Lex 更好则接受尾巴并继续打这个 seed；否则回滚 snap; MIP 未证明最优则 not-proven 计数，超 miss_fail_max则 freeze，否则 bump seed_scale 再试：最优但仍没打下 first bit 则 conf+= /（cover）， confe1 则 bump scale再试，conf>=1则 freeze_seed. accept: first bit 严格变小，热结构变了，所有「这条弧打不动」的记忆作废。accept-tail:first bit不变后面某位变好，keep-hot。回滚用 snap.patch（IE wp）。freeze_seed 总是 skip+focus_donei test_key 说 outdeg1/bridge 则 pre_frozen 并可抬 1b_sat；否则若在 epoch_focus 里则 soft_frozen；不在 focus里只 skip,epoch 结束 skip 会清。cover=候选路径用到的不同弧数/总弧数；miss_delta=cover**0.25；cover 必须由builder 写入 timing［"cover"］，漏写则 conf恒0、同一弧打到死。未证明（超时/infeasible）走 not_proven_n，一miss-fail-max 2 次就 freeze，防止无限加倍 scale. bump_scale 使该 seed 下次 mip-budget 与 n_hot_max xgrow_mult，封顶 scale_cap；
reschedule 时全局 search_scale 同样乘。16.MIP gurobi/builder.py xlt,di, kJE｛0,1）表示 demand di在时段t用候选k，同一 owner的 sticky 槽共用 x。2是当前层要压的最大饱和，Lb=H。sat ［t,arcl*cap mn frozen_load +E vol*frac*x。非冻结、非 pin 的弧 sate=z。每个（t,di）恰好 zk x=1.optimize（lex_depth）： min zi 记下 zhat 并把 satezhat 的弧锁死 sat c=zhat；可选第一层后改目标为 min Hamming（-min-hamming; setB 当前关掉）：新z对剩余弧再 min z，最多 lex_depth 层（默认8）；无解则保持 incumbent, Graph 上 pin 的弧1b=ub=负载，不进 max（z）。mip_budget 限制的是估计 n-pairs*pool_est，不是 Gurobi真正的 0-1个数；超16000条路径从袋子尾部砍。17.Graph单元 solution/graph/py/graph.py Numba 图提供 BFS 跳数、出度、pin界。graph_unit.graph_from_data 注入 SpCache.graph。Presolve 出度走 Graph。候选主路径仍用 Python Dijkstra + SpCache.18.Liveview tasr/liveview.Py —liveview-port 8765 -Liveviev-top 12：本机 HTTP,/api/status 为JSON，页面 8.5s拉一次。每个t一格，格内 M 根竖柱，柱高=sat，无背景槽。蓝刻度=1bsat；琥珀帽=conf；红描边=当前 MIP seed；绿字=focus t；灰=frozen。页顶 score/top10显示 mluG和官方向量前10位（带t、arc）。ReuseHTTPServer仍可能 bind 失败，后续实例打liveview bind failed，求解维续。19.入口与推荐参数 单实例：solution/main.PY -net -tm --scenario-srpaths. setB当前常用：m1x-first-n-hot 1--n-hot-max 16—mip-budget 5000 --pooL-est 24 -no-min-hamming
-budget-mode first_half -max-rounds 0 --wall-limit 600 --schedule pingpong --sticky-span 1 --no-halo —Presolve unmovable —presolve-time 15 -focus-n 10-grow-mult 2--scale-cap 16 --miss-fail-max 2--liveview-port8765 liveview-top 12. setA 试跑可用 WaLl 605.presolve-time 5, Gurobi CS 用 GRB_CSMANAGER / GRB._USERNAME / GRB_PASSMORD GRE_CSAPPNANE， 输出 （nane）rsrpaths.json （只写非空 weypoint）和 summary_*-1son
20.主数据流
json-DataC-Graph+SpCache+Snapshot wp)-Presolve tf: loop: Snapshot, rank-schedule it t-decompose_hot-mix 182-Builder MIP lex-snapshot.patch-evaluate trunco-accept/accept-tail/rollback-confEt not-proven-skip/
freeze-LiveView. update；最后 write_srpaths。21，注意要点 官方分是整条 sat 降序 trunc6 的lex，不是只比mlu，也不是四舍五入。H必须等于冻结 demand 的真实负载，算错会虚降2。Hamming rhsz0，负 rhs 直接 infeasible. infeasible 保 incumbent，不要当崩溃。cover 必须写入 timing，否则 conf 恒 0,unproven 与 optimal-miss 分开：前者 miss_fail_max，后者 conf.scale 到 cap 就停，不要解冻再打第N+1峰。只 soft-freeze epoch_focus 里的key。 focus-N 不要 allow 过滤分解。reschedule必须把 early 游标归零。奇偶：偶=hot，奇=early。候选路由不要 ban 热弧再拿 frac, sticky 会占 Hamming; first_half 给第 1轮留预算。min-hamming 在setB上常更差。跨t热弧一起进 MIP 会爆Y变量、dynl用 same_t_first。 setA-01 first bit 0.929383是 Sprint 最优，empty 的 1.0 才是能轻易拉下来的那截。LiveView 每实例 bind 一次，多实例串跑注意端口。patch 后必须用 evaluate_snapshot，不要以为MIP的2就是全局lex。输出节点 id是原始1d，内部路由用 0..n-1。wall imit在 round边界检查，一轮MIP可能略超。22，测试 cd solution 后 graph/py/，venv/bin/python -m pytest tests/test_decompose.py tests/test_presolve.py tests/test_candidates.Py -9。覆盖分解、dynl、pingpong 奇偶、presolve、mix随机注入